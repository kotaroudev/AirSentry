# AirSentry System Dependencies

AirSentry is Linux-first and uses low-level kernel interfaces when possible.

## Recommended system tools

These tools are not Python packages and must be installed using the system package manager.

### Debian / Ubuntu

```bash
sudo apt update
sudo apt install -y iw bluez iproute2